package com.rossomak.flashcards.core.domain.model

/** A runtime permission the app asks the user for, named in domain terms rather than manifest strings. */
enum class AppPermission {
    RecordAudio,
}
